# Local Agent Adapter Architecture

> **DEFERRED — Codex parity phase: local-agent integration research is
> retained but not under implementation; see operator directive §17; do not
> expand during parity phase.**
>
> Original location: unknown — the research was produced in a pre-reset
> session and was never committed to any repository (see §Status below).

## Status

This file is an **honest stub**, created by Worker C1 (Task 41-C1) on
2026-09-16 during the parity-wave framework build:

- Local-agent adapter architecture research **existed in a pre-reset
  session** (prior to the platform resets recorded in the shared worklog),
  but it **was not committed** to `payswapdotorg/Flauz.app`,
  `payswapdotorg/codex`, or any surviving location, and the sandbox resets
  destroyed the working copies.
- A search across `/home/z/codex/docs`, `/home/z/Flauz.app`, and
  `/home/z/my-project` (filenames and content, `local.agent|local-agent`)
  found **no surviving record** of it.
- Per the operator directive (§17), the topic is **retained but deferred**:
  no adapter architecture is under design or implementation during the
  Codex parity phase, and this document must not be expanded with
  speculative content. In particular, no adapter architecture may be
  inferred from the current Flauz codebase and written here as if it were
  the recovered research.

## Where related material lives

- `docs/prior-art.md` (this repository) — prior-art survey and reuse
  decisions, including integration-boundary references.
- `docs/architecture.md` (this repository) — the current, committed Flauz
  architecture (app-server boundary, native client).
- The `payswapdotorg/codex` repository research/documentation directories
  (read-only for the parity wave): `docs/architecture/`,
  `docs/development-state/`, `docs/product-delivery/`, `docs/work-orders/`,
  `docs/validation/`.

Do not invent content for this file. If the original research is ever
recovered (operator archive, chat export, untracked backup), append it below
this line with its provenance and remove the stub notice — the DEFERRED
banner stays.
