# Codex Desktop parity matrix

codexRS is a native Rust replacement for Codex Desktop. The release target is
behavioral and UX parity without Electron, Tauri, Wry, WebView, Node.js, or a
browser runtime.

This matrix is the contributor-facing source of truth. A row is complete only
when the visible behavior works through the official app-server or a native
platform boundary, has focused regression coverage, and has been compared with
the reference UI at the same state and viewport.

## Reference baseline

| Source | Pinned reference |
| --- | --- |
| Windows package | `OpenAI.Codex 26.721.3996.0 x64` |
| Desktop bundle | build `5828`, internal version `26.721.31836` |
| Bundled Codex CLI | `0.146.0-alpha.3.1` |
| CLI SHA-256 | `39e9e041ea33ac34aad9578adfe660c5c7a6dc8f82620b77623960f9352a6ef3` |
| Stable app-server schema | 89 client request methods |
| Experimental schema | 126 client request methods |
| UI inspection date | 2026-07-25 |

Evidence comes from:

- the official OpenAI feature documentation and product demo;
- a read-only inspection of the locally installed `app.asar`;
- schemas generated by the pinned official `codex app-server`;
- focused probes through an official supervised app-server process;
- the current codexRS source and native UI.

Extracted upstream code and assets are research material only. They stay under
ignored `target/` paths and are never redistributed or committed.

Status values:

- `done`: usable end to end and covered by the relevant release gates;
- `partial`: a real vertical slice exists, but the reference exposes more;
- `missing`: no usable end-to-end implementation yet;
- `platform`: implemented on only part of the supported platform matrix.

The stable renderer contains `foregroundClicks`,
`foregroundAndBackgroundClicks`, and `off`, but the inspected stable main
process reads and writes that mode through macOS `/usr/bin/defaults` only.
Windows returns no mode and the setter is a no-op, so click sound is not a
Windows/Linux parity gap.

The inspected stable main process opens the primary window at `1280×820`,
enforces a `480×600` minimum, persists normal bounds plus maximized state under
`electron-main-window-bounds`, clamps undersized saved values, and restores
only placements intersecting a current display work area.

The stable renderer uses a 275 px sidebar, closes competing right-side panels
at 960 px or below, and closes the adaptive sidebar at 720 px or below. Explicit
sidebar toggles suspend that automatic behavior, while right-side panels are
restored after the viewport widens again.

Its shared dialog surface caps width at 92% of the viewport. Tall stable
dialogs additionally reserve a 16 px inset on every side and scroll their
content instead of escaping the window.

The stable Windows shell uses a 34 px client-decorated title bar with the
sidebar, Back, Forward, File, Edit, View, and Help controls followed by 46 px
native minimize/maximize/close hit areas. Its menu contract and accelerators
were recovered from the packaged stable main/renderer bundles without
modifying or extracting the installed app.

## Product parity

| Area | Status | Reference contract still required |
| --- | --- | --- |
| Runtime bootstrap | done | Keep the exact packaged CLI hash check and explicit override/fallback order |
| App-server supervision | partial | Startup declares the stable Desktop experimental/form-elicitation/attestation capabilities and exact legacy-notification opt-out contract. A bounded `thread/loaded/list` page is hydrated through `thread/read`, prioritized in the task list, and resumed after connect so loaded/background sessions recover active-turn state and subscriptions. Unexpected local exits use one deduplicated `1/2/4/8/16/20`-second reconnect timer, reset only after successful initialization; the native footer exposes pending/running attempt state and a bounded safe failure tooltip, while initial startup failure remains explicitly retryable. The timer progression, repeated-loss race, blocked-restart path, and automatic return online were exercised against the pinned CLI in an isolated `CODEX_HOME`. Complete the remaining stable methods plus remote and network-aware transport diagnostics |
| Projects and chats | partial | Grouping, bounded full-text chat search with stable `thread/search` snippets and pagination, the unified command menu and focused chat-search flow, archive/unarchive/delete, rename, and codexRS-owned bounded pinning with `thread/read` hydration work. The selected-chat overflow now follows the stable order and exposes a native `Copy` flyout with `Copy working directory`, `Copy session ID`, `Copy deeplink`, and `Copy as Markdown`; the sidebar context menu keeps the stable flat three-item copy section and does not expose Markdown. Working-directory copy uses the displayed non-verbatim path and exact success status, while deeplinks use `codex://threads/<session-id>`. Stable `Open folder` now sits between `New chat` and `Search files`, uses Ctrl/Cmd+O and a native single-directory `Select Project Root` picker, rejects non-absolute paths, remembers the selected local project in codexRS-owned storage, restores the newest still-existing project after restart, and prepares the next official app-server chat with that explicit `cwd`. `New standalone chat` follows the stable Ctrl/Cmd+Alt+O and command-menu contract, clears both the active and remembered project for that draft, and starts the official app-server thread without `cwd` or `runtimeWorkspaceRoots`. A bounded 64-entry local-project registry now lives in codexRS-owned SQLite with v1/v2-to-v3 migrations and independently renders projects with zero chats. `Add new project` uses the existing native picker; project rows support selection, new chat, rename, pin/unpin ordering, platform file-manager opening, missing-folder warning, and removal from the app without deleting files or chats. Exact rename/remove dialogs, persistence across restart, pinned ordering, and independent overflow actions were exercised through Computer Use against an isolated profile. Add multi-root sources, manual project ordering, richer metadata, and unread state |
| Thread execution | partial | Start, steer, interrupt, reconnect, selected active-turn restoration, editing the latest user message, and manual context compaction use typed stable methods. Edit follows the stable `thread/rollback` + replacement `turn/start` flow, preserves supported local image/mention/skill attachments, retries without applying rollback twice, refuses unsupported external attachment shapes, and opens from the stable `Edit` action or double-click. `/compact` follows the stable `thread/compact/start` contract, is idle-only and single-flight, renders `contextCompaction` items with the stable in-progress/completed labels, and shows the exact rounded context percentage from `thread/tokenUsage/updated` using `last.totalTokens / modelContextWindow`. Stable `model/safetyBuffering/updated` now drives the native waiting banner only for the active submitted turn, preserves accepted steer messages in a bounded retry snapshot, disables retry once assistant output starts, and offers the exact faster-model, dismiss, and learn-more actions. Faster retry interrupts the source turn, validates the latest two turns through bounded `thread/turns/list`, forks immediately before the interrupted turn with deferred goal continuation, inherits Computer Use, and resubmits on the advertised model with low effort; stale, in-progress, interrupt, read, fork, and start failures remain bounded and restore the prompt when needed. The banner and complete source → fork → faster response path were exercised in the built native app against an isolated app-server fixture. Stable `model/verification` is typed and surfaces the exact Trusted Access for Cyber warning as a native, bounded timeline card with a validated program link while discarding provider-only metadata. Explicit `/shell` commands now use stable `thread/shellCommand`, preserve shell syntax, work as standalone or active-turn auxiliary actions, reject attachments, disable irrelevant composer controls, and disclose that execution has full system access outside the thread sandbox. The pinned app-server returned the empty acknowledgement and streamed the expected command item and completed turn from an isolated `CODEX_HOME`. Stable chat-memory execution uses typed `thread/memoryMode/set`; explicit new-chat `generate_memories` and `use_memories` choices are forwarded through `thread/start.config`. Both paths returned successful acknowledgements from the pinned app-server in an isolated `CODEX_HOME`. Add automatic/manual compaction provenance and richer edit metadata |
| Composer | partial | File/folder/image attachments plus Plan, Goal, Send, Steer, and Stop states work; Goal mode works in new and existing chats, including file-backed Goal attachments through the normal public starter-turn path. New chats retain typed file/folder/image inputs at thread creation; existing idle chats send the exact `/goal` starter turn and call `thread/goal/set` only after `turn/start` is accepted; active chats preserve the draft instead of steering Goal attachments. Active goals use the stable 250 ms guarded continuation, Stop pauses them before interruption, completed goals remain attributed to the final response, and the active Goal tray/footer follow the stable layout. The exact stable unassigned shortcut actions for Send message, Add photos, Attach files and folders, Toggle Fast mode, Increase/Decrease/Cycle reasoning effort, and Toggle plan mode are editable and reuse those native paths. Add photos uses a native multi-file picker and accepts the stable PNG/JPEG/GIF/WebP extension set before forwarding bounded local-image paths to the existing attachment pipeline; reasoning steps follow the catalog order, clamp at either end, and wrap only for Cycle. Exact `/chat`, `/compact`, `/feedback`, `/fork`, `/goal`, `/init`, `/mcp`, `/memories`, `/model`, `/new`, `/plan`, `/project`, `/reasoning`, and `/status` command paths share the existing native actions; `/chat` uses the exact stable title and description, appears only while a local project is active, and reuses the existing projectless-chat transition, `/goal` opens the existing goal editor, `/model` and `/reasoning` open their catalog-backed native pickers, `/plan` toggles the same composer mode and exposes its current on/off action, `/new` retains the selected workspace, `/feedback` opens the native Share feedback dialog, and `/compact` reports live context-window usage when the app-server provides it. Local idle workspaces expose `/init` with the exact stable title, description, 1,744-byte CRLF prompt, and existing-file guard; it uses the normal native turn path and reports the exact stable skip copy instead of overwriting an existing `AGENTS.md`. `/mcp` opens a native inline submenu backed by the existing typed MCP refresh path, sorts and bounds configured servers, and uses the exact stable Loading/Error/Empty, Enabled/Disabled, and authentication labels without exposing raw configuration or provider errors; Escape returns to the slash list. `/memories` is feature-gated, reports exact new-chat `Use on/off, generate on/off` or current-chat `Generate on/off` summaries, and opens the stable native `Chat memories` dialog. New-chat switches stay local until `thread/start`; existing chats lock `Use memories` and apply `Generate memories` through typed `thread/memoryMode/set` with optimistic rollback on failure. `/project` uses the exact stable `/` and `@` triggers, title, description, empty state, and inline local-project picker; it derives at most 64 unique absolute workspace roots from the existing bounded local task state, prioritizes the current draft and selected task, and reuses `SelectWorkspace` to prepare and remember the selected project for a new official app-server chat. The exact stable `composer.openProjectPicker` command uses `Open project picker`, `Open the composer project picker`, and Ctrl/Cmd+Alt+Shift+O; it is enabled only in the new composer, opens the same bounded native project picker, and preserves the current draft, attachments, and composer mode on selection or Escape. `/status` opens the stable-shaped native panel above the composer, copies the selected session ID with the exact tooltip transition, reports live context tokens and remaining percentage, sorts bounded account windows, and uses the stable duration, reset, and unavailable copy without exposing provider errors; Close or Escape dismisses it. The growing menu is viewport-bounded and scrollable, including all fifteen current commands at the stable minimum window size. Cursor-aware native `@` discovery uses the exact typed `fuzzyFileSearch/sessionStart`, `sessionUpdate`, and `sessionStop` lifecycle, recovers missing sessions, falls back to the legacy request when required, bounds roots/queries/results, confines returned paths to known roots, applies the stable generated-directory exclusions, and supports keyboard or pointer selection of the project command, file attachments, and directory completions. Stable catalog mentions load through bounded typed `plugin/list`, paginated `app/list`, and native installed-app discovery, and render in exact `Plugins → Desktop apps → Apps → Skills → Files` order. Enabled plugins plus the installable Browser and Computer specials use `plugin://<id>`; enabled accessible Apps use `app://<id>` with an empty-query cap of three and duplicate plugin-backed aliases removed; Desktop apps appear only for a non-empty query with Computer available and use `plugin://<computer-plugin-id>?app=<URLSearchParams-encoded-id>`. Selection removes only the active `@token`, preserves surrounding text, validates and deduplicates chips, and serializes App/Plugin chips into the first text input as stable Markdown links while keeping `text_elements: []`. The protocol and UI were exercised against the pinned official app-server and isolated native fixtures, including stale updates, unsupported separators, `Escape`, exclusion of an app-server-returned `node_modules` result, new-chat `/init`, the existing-`AGENTS.md` guard, the MCP empty state, submenu return, full-menu scrolling, the complete Status session/context/two-window/copy/Close/Escape paths, `/project` through both triggers, picker return, selection, and workspace handoff at 1280×820 and 480×600, `/chat` availability, exact copy, project-to-projectless handoff, and disabled state at 480×600, and App/Plugin/Desktop-app mention filtering, keyboard selection, pointer selection, chip rendering, preserved text, and scrollable short-window layout at 1280×820 and 482×602. The `/memories` new/current-chat summaries, exact modal copy, locked `Use memories`, feature gate, and Personalization reset confirmation were also exercised through Computer Use against isolated profiles; destructive Reset was validated separately through the official app-server protocol instead of being clicked in the UI. For a selected local Git chat, the separate lower footer provides the stable-shaped `Continue in` Work picker with checked `Work locally` and `New worktree`; the latter explains `Create a copy of your local project to work in parallel` and reuses the existing guarded worktree-fork flow. Native Computer Use in an isolated `CODEX_HOME`/data profile exercised menu open, the exact labels and description, and Escape close at 1280×820. Add cloud projects, the remaining stable slash commands, Cloud target, and voice |
| Model, effort, and speed picker | partial | Catalog-backed model, reasoning effort, and Standard/Fast/Ultrafast service-tier selection works; `config/read` and managed `configRequirements/read` defaults feed new chats, profile-aware `config/batchWrite` persists unlocked new-chat defaults, `thread/resume` restores existing chats, `thread/settings/update` applies changes immediately, and thread/turn starts carry the selected tier. The searchable model picker preserves compact rows for empty metadata and renders bounded public `model/list.description` values as muted second lines without changing selection IDs or keyboard behavior. An empty catalog now distinguishes loading, ready-empty, and failed states in the disabled selector and closes a stale open menu when a previously populated catalog becomes empty. Available model tiers also produce bounded stable `service-tier:<id>` slash rows: Fast uses its catalog description while inactive, shows the exact `Turn off Fast and return to standard speed` copy while selected, and reuses the same typed `SelectServiceTier` path in both directions. The exact stable editable `Ctrl+Shift+M` app shortcut focuses and opens the existing native searchable model picker when its catalog is available. Selected models with public `upgradeInfo.upgradeCopy` show a bounded native notice and optional validated `modelLink`; `migrationMarkdown` remains omitted. Public `availabilityNux.message` opens a bounded native introduction modal for the first eligible unseen catalog model, remembers dismissal in codexRS-owned storage, and reuses the typed model-selection path. Standard/Fast toggling, selected copy, dynamic menu placement, and all four footer controls were exercised against an isolated native fixture at 1280×820 and 480×600. |
| Permission profiles | partial | Catalog-backed built-in/custom selection matches the stable Ask for approval, Auto-review, Full access, and Read only presets and renders the available catalog descriptions in the native picker; `configRequirements/read` filters managed restrictions, `config/read` supplies new-chat defaults, and `thread/resume` plus `thread/settings/update` restore and immediately apply the selected thread's profile, policy, and reviewer. Add sandbox detail, the granular/custom editor, and per-project resolution |
| Streaming timeline | partial | Bounded paginated history now uses a bottom-aligned variable-height native list. Stable-style compact `Explored`, `Ran`, file-change, web-search, subagent, image-inspection/generation, and completed-turn background-terminal summaries keep streamed output out of the summary and expose bounded expandable command/diff/source/detail rows. Stable-shaped generated-image events use a 400 px rounded placeholder while in progress and a clickable 400 px preview after completion; previews reuse the bounded workspace-confined image loader and open the native Outputs viewer. Stable `sleep` and collab `wait` items stay hidden. Assistant responses use selectable native GFM Markdown with syntax-highlighted copyable code blocks; source-range Find highlights preserve links, nested formatting, and code styling, while external Markdown/HTML images and unsupported link targets are neutralized before the renderer can open them. Stable legacy and directive file citations render as inline chips, structured memory citations retain bounded notes, and opaque web/image-generation results never expose raw provider payloads. App-server turn errors are reduced to bounded user-safe status text instead of exposing raw provider messages or metadata. Stable `::code-comment` directives render bounded findings with priority, body, file/line, and Changes-inspector navigation when the path is already in the current diff. User and assistant messages no longer use fixed-height cards. The stable `Copy as Markdown` action automatically loads bounded remaining history, exports the title, quoted user turns, assistant/plan text, expandable activity, attachments, and generated images, omits reasoning and host directives, and uses the exact success/failure status copy; repeated cursors, offline history, page limits, and the 4 MiB UTF-8 cap produce an explicit truncation notice. Add adjacent activity grouping, internal file navigation from citation chips, turn-level source aggregation, subagent navigation/grouping, and exact history-position restoration |
| Approvals and user input | partial | Command, file, permission, and dynamic-tool approvals are native. Stable public `item/commandExecution/requestApproval`, `item/fileChange/requestApproval`, and `item/permissions/requestApproval` use bounded typed payloads and exact renderer decisions: one-shot/session command approval, validated execpolicy/network amendments, one-shot/session file approval, and turn/session permission grants. Non-renderable permission requests are auto-denied with a successful empty profile just like stable. Exact card identity, titles, details, button labels, scoped menus, network allowlist action, special-path composition, and typed JSON responses were exercised end to end against an isolated app-server fixture. The exact editable `approval.approve` (`Enter`) and `approval.decline` (`Escape`) commands resolve only the request visible for the selected chat; they yield to native modal, popup-menu, select, MCP-elicitation, and structured-input focus contexts instead of approving a background request or swallowing another blocking surface. URL-mode `mcpServer/elicitation/request` matches the stable interruption flow with bounded typed input, validated HTTPS links, `Action required`, `Open link` → `Continue`, `Decline`, focused `Escape` decline, exact accept/decline responses, and duplicate/queue guards. Standard form mode maps bounded JSON Schema into native string, number/integer, boolean, single-select, and multi-select controls; applies defaults and stable required/format/range validation; and returns exact typed actions and accepted content. Extended `openai/form` supports mixed standard fields and bounded `openai/imagePicker` data-image carousels with intrinsic aspect ratios, stable single-picker header suppression, immediate required-state gating, previous/next controls, and exact `accept` item IDs, `decline`, and `cancel` responses. Schemas this native client cannot safely display use the stable 28 px unsupported-form surface with exact copy, danger `Dismiss`, primary `Skip`, and Escape cancellation instead of being silently declined. Official `item/tool/requestUserInput` requests use the stable immediate-response card with first-option defaults, numbered choices, Recommended badges, multi-question navigation, keyboard selection, Other/freeform input, `Skip`/`Next`, close/Escape empty responses, and exact keyed answer serialization. Its host-side behavior also matches stable's 60-second foreground inactivity period, 90-second background/countdown resolution, final-minute countdown chip, and permanent snooze after card interaction. URL, standard form, image-picker, unsupported, structured-answer, Skip, Escape, and real timeout paths were exercised end to end against isolated app-server fixtures. Add the remaining connector-specific request methods and persistence choices where the public contract exposes them |
| Repository status | partial | The native repository snapshot exposes the current branch, ahead/behind state, bounded changed files, local branches, worktrees, and at most 30 commits unique to the current branch. The stable-shaped wide `Uncommitted changes` surface separates exact `Unstaged` and `Staged` counts and file sets. Branch switching and create-and-checkout use bounded native Git operations and surface working-tree overwrite conflicts without force or implicit stash. The stable-shaped Commit dialog persists `Include unstaged changes`, bounds generated context to 20 KiB, uses an ephemeral read-only `gpt-5.6-luna` app-server thread for blank-message generation, preserves native Git hooks, and exposes the exact `Commit`, `Commit and push`, and `Push` action list. Push uses a bounded supervised `git push --porcelain`, keeps an existing upstream, and otherwise publishes `HEAD:refs/heads/<branch>` with `-u`; generated commit-and-push and clean ahead-only push were exercised against a local bare remote at the reference viewport. The Changes inspector exposes the complete stable source selector: bounded completed-turn changes through `Last Turn`; all staged, unstaged, and untracked changes through `Uncommitted`; exact file scopes through `Unstaged` and `Staged`; one selected branch commit through `Committed`; and `Branch` with the exact current-to-base header, a searchable 30-item recent-branch picker, remote-qualified default, selected check, and typed `Use <ref>` path. The Repository route preserves the user's Split preference on wide windows but renders and truthfully selects readable Unified rows at compact and narrow widths, restoring Split automatically after widening. The default target retains the official app-server `gitDiffToRemote` contract; an explicit target uses a bounded native `merge-base HEAD <ref>` diff that also includes untracked files. Add pull, guarded force-push UX, richer conflict resolution, repository picker, and multi-repository projects |
| Diff review | partial | Selecting an `Unstaged` or `Staged` file issues only the matching bounded Git query and renders the patch with real old/new line numbers, hunk headers, and added/removed colors; switching sources invalidates the previous selection. Bounded `--numstat -z` metadata supplies per-file `+/-` totals without line-based path parsing. Untracked UTF-8 files use a bounded, workspace-confined added-file patch; binary and oversized content remain bounded. The selected file can be staged or unstaged, and the active section exposes exact `Stage all` / `Unstage all` actions. The compact stable-shaped selector groups `Last Turn`, `Uncommitted` / `Unstaged` / `Staged`, and `Committed` / `Branch`; the commit submenu shows bounded subjects, full-message tooltips, relative times, and the selected check. `Last Turn` renders the latest bounded app-server file-change patch without Git operations. `Uncommitted` combines tracked and untracked working-tree changes, while `Committed` isolates the selected commit and falls back to `Branch` when a refresh removes it. `Branch` loads the official remote-base diff with loading, retry, empty, failure, truncation, refresh, and stale-response guards; whole-source modes expose no staging actions. Public `review/start` launches bounded `UncommittedChanges` or `BaseBranch` review with the persisted `Inline` or `Detached` delivery preference. The full Repository route also offers bounded Unified/Split review. Stable `Toggle Review panel` (`Ctrl/Cmd+Alt+B`) hides the active right-side inspector or right-docked terminal and restores that same tab without creating a terminal session; with no remembered tab it opens Changes. The separate full-width control and `toggleMaximizeSidePanel` action collapse the chat surface, expand the active Changes/Outputs/Files/Computer panel across the workspace, expose exact `Expand panel` / `Restore panel width` labels, and reset when the panel closes or responsive layout hides it. The scoped Changes view, empty staged state, shortcut, command-menu path, and restoration of the Computer Use tab were exercised against an isolated fixture at the reference viewport. Add multi-file expansion, syntax parity, inline comments and handoff, per-hunk actions, and guarded revert controls |
| Branches and worktrees | partial | The composer and Repository route expose local branches plus the stable create-and-checkout dialog. A listed non-bare worktree can become the explicit workspace for a new chat; the resulting official `thread/start` receives both `cwd` and `runtimeWorkspaceRoots`, and the selected path is visible before the first message. `/fork` now follows the stable destination submenu: a normal checkout lists `Continue in new worktree` before `Continue in new chat`, an existing linked worktree reverses that order, and the task context menu exposes both destinations. Current-workspace forks send the source `cwd`; managed forks allocate a detached worktree under codexRS-owned storage, reproduce tracked, untracked, `.worktreeinclude`, and ignored `AGENTS.override.md` state within a combined 20 MiB limit, preserve nested workspace roots, then send the new `cwd` through the pinned `thread/fork` contract. Managed forks use the stable-shaped activity flow (`Creating a worktree` → `Worktree created` → `Starting the conversation`) and a bounded three-item FIFO shared by `/fork`, task menus, and the composer Work picker. Exactly one end-to-end handoff runs at a time; every queued request snapshots its source and root, all asynchronous transitions are request-correlated, queued requests can be cancelled immediately, and active cancellation waits for the supervised Git worker to stop before advancing. Failures block the queue with bounded Retry/Dismiss controls, while a failed conversation retry reuses the already-created worktree instead of duplicating or deleting it. The branch dialog, listed-worktree handoff, protocol serialization, nested working-tree transfer, cancellation/recovery reducer, queue bounds, and title/Computer Use inheritance have focused fixture coverage. Add guarded removal and cleanup policy, setup scripts/local environments, synced branches, richer worktree status/repair, and remote worktrees |
| Pull requests | partial | The stable-shaped 420 px Create PR flow preflights the bounded GitHub CLI, including `gh --version`, authenticated-status fallback, and an existing open PR lookup for the current branch. It exposes the exact title, generated-description, commit-and-push checkbox, draft, ready, browser, phase, provider-error, and existing-PR/View states. Blank metadata uses a bounded ephemeral read-only `gpt-5.6-luna` app-server thread; local changes can generate one combined commit/PR result, commit through native Git hooks, publish the upstream, and invoke the exact bounded `gh pr create` arguments. The exact stable Git command IDs route Commit or push, ready/draft PR creation, branch creation, linked-PR browser opening, and authored linked-PR merge options into those existing native surfaces; ready versus draft creation also controls the modal's default keyboard action. Merge submission now requires an authored, open, non-draft, currently mergeable PR with a clean/allowed merge state. The global native `/pull-requests` route follows the stable current-user GraphQL contract through supervised `gh api`: search text is capped at 256 characters, pages at 100 items, accumulated results at 1,000, and all process output is bounded. Reviewing/Authored and Open/Merged/Closed filters, debounced quoted search, cursor pagination, stale-generation guards, result states, account identity, selection, and the persistent two-pane layout are wired. Selection loads bounded Summary, Timeline, Checks, and Code data with validated repository identity and HTTPS URLs; the native tabs render description, review/branch/stats metadata, individual check runs, paginated activity, and a bounded diff. General comments, approve/comment/request-changes reviews, and merge/squash actions use the stable CLI contracts; every review and merge first verifies the currently loaded head revision, and draft PRs keep merge disabled. Authored open PRs expose the stable `Draft`/`Ready for review` selector plus guarded `Edit title` and `Edit description` modals; all three revalidate the loaded head before invoking exact bounded `gh pr ready`/`gh pr edit` arguments and refresh detail after success. The comment and approval modal flows were exercised end to end through the native UI against an isolated bounded mock-`gh` fixture, including the exact spawned arguments. Persisted Git defaults now control force-with-lease pushes, draft defaults, merge emphasis, and generated commit/PR instruction context. Add automatic linked-PR detail loading for merge shortcuts, the GitHub connector path, detached-worktree branch creation, reviewer conversation threads and inline replies, and the remaining guarded GitHub mutations |
| Terminal | partial | Bounded per-chat terminal tabs run as independent native PTY/ConPTY sessions in a bottom or right dock. Opening Terminal starts the first session when needed; new-tab, selection, close-one, stop, restart, bounded input/output, truncation, and exit state are scoped by session ID. Tabs prefer the bounded shell-emitted title and otherwise use the stable cwd/index fallback. Hiding the dock preserves live sessions and reopening restores the active tab; closing the final tab closes the dock. The stable `Toggle bottom panel` remains distinct from `Open terminal`: Ctrl/Cmd+J only shows or hides an existing Bottom tab, never creates a session, and does not affect a Right dock. The stable `Default terminal location` setting persists `Bottom` or `Right` in codexRS-owned storage. Native drag handles enforce the stable bottom defaults and bounds (280 px, minimum 160 px, maximum half the viewport) and right defaults and bounds (600 px, minimum 320 px, with 352 px reserved for the main view); the final height and width persist once per completed drag and are clamped again for the current viewport when restored. A right dock now follows the stable shell breakpoints: it hides at 960 px or below, restores after widening, and can be explicitly reopened as a full-width panel below the compact task header. On Windows, the stable `Integrated terminal shell` setting exposes PowerShell and Command Prompt plus detected Git Bash and WSL installations. PowerShell prefers `pwsh.exe` and falls back to Windows PowerShell, Git Bash uses `--login -i`, unavailable choices fall back through PowerShell to `cmd.exe`, and each tab retains the shell it was created with across stop/restart while later tabs use the newly persisted choice. Two-session isolation, restart, close-one, hide/reopen, persisted placement, panel sizes, and shell choice across restart, mixed Command Prompt/PowerShell tabs, both layouts, resize bounds, and right-dock behavior at 1280, 800, and 480 px were exercised against an isolated fixture. Add generic movable panel tabs, session transfer, and workspace-binding warnings |
| Process Manager | partial | `Ctrl+Alt+M`, the command palette, and completed background-terminal activity open the native stable-shaped manager for the selected chat. Bounded paginated `thread/backgroundTerminals/list`, per-process `terminate`, and chat-wide `clean` use the supervised official app-server; loading, unavailable metrics, errors, empty state, refresh, stop-one, and stop-all states are implemented. The recovered stable one-second cadence now refreshes only while the dialog remains open, coalesces in-flight list requests, pauses during stop/clean mutations, and terminates on close. The stable minimum column widths remain available through horizontal scrolling when the 92%-viewport dialog becomes narrower than the table. Its isolated render frame prevents the Windows debug UI stack exhaustion observed during the `480×600` smoke pass. The empty state, compact footer, close path, and full-size table were exercised against an isolated fixture at both minimum and reference viewports. Add runtime/status values when the official protocol exposes them and cross-chat aggregation if an upstream contract becomes available |
| Computer Use | partial | Native Any App emits stable case-preserved AUMIDs, known-folder GUID IDs, or absolute executable paths, rejects oversized IDs and shared hosts, excludes self-control, asks before every app's first read/control call, and manages the app-server-owned Windows allowlist in the stable table format and Settings surface. The stable-derived product-policy guard blocks Codex, terminal, password-manager, identity, and security targets before approval, after delayed approval, and inside the supervised helper, so an allowlist entry cannot bypass it. A bounded native catalog lists and launches Start Menu shortcuts, execution aliases, packaged apps, and AppsFolder entries; exact Shell AppUserModel IDs, link target paths, full UserAssist timestamps, the stable 24-hour in-process cache, 40-item cap, and running-first merge preserve the recovered stable ordering contract. Non-empty composer `@` queries expose the same catalog under `Desktop apps` through the active Computer plugin's stable URI. The exact stable Window2 surface is wired in its published order: `list_windows`, `get_window`, `list_apps`, approval-gated `launch_app`, `get_window_state`, `click`, `press_key`, `type_text`, `scroll`, `set_value`, `drag`, `perform_secondary_action`, and `activate_window`. Every action rehydrates and validates its discovery-returned Window. Stable-aligned control covers bounded state/screenshot selection, screenshot-ID coordinate remapping, automatic target activation, pointer input, X keysym-style chords including numpad aliases, and Windows accessibility trees, indexed clicks, value replacement, and secondary actions through a supervised native Rust helper. Common Windows browsers now use the recovered stable URL-policy path: a bounded locale-independent UI Automation `Document.Value`, legacy `getAuthStatus` through the supervised official app-server, authenticated `/backend-api/aura/site_status`, one refresh-on-401 retry, a second URL read against navigation races, and the exact stable stop copies. A policy failure blocks later Computer Use calls through that turn and the same guard runs before and after delayed app approval. The native Edge fixture verifies exact URL extraction without reading a live browser profile. A bounded native Windows interruption monitor arms only between Computer Use calls, ignores a pre-held Escape key, disarms around agent input, interrupts the exact app-server thread/turn on a fresh physical Escape press, rejects later Computer Use calls in that turn with the recovered stable stop copy, and cancels pending app approvals before interrupting. Fresh foreground cursor movement, mouse-button edges, and common keyboard edges invalidate that window's latest screenshot and reject later actions with the recovered stable `call get_window_state` copy until a new bounded state is captured. Guarded input now synchronously raises a native capture-excluded, click-through, no-activate, topmost virtual-desktop indicator through the shipped `codex-computer-use-overlay.exe` companion before acting. Its bounded JSON-lines IPC, kill-on-close Job Object, and fixed sibling path reproduce the stable separate-helper boundary without invoking or redistributing the official binary; it uses the recovered `#339cff`, 56 px offset, 920 px width cap, 8 px radius, `Segoe UI Variable`, exact `ChatGPT is using your computer` / `Esc to cancel` copy and accessible title, persists through the Computer Use turn, and closes on completion, physical Escape, URL-policy stop, disconnect, or shutdown. Indicator failure blocks input. Add the remaining bounded registry catalog sources, persistent 24-hour owned cache parity, broader browser identification, low-level wheel/touch and injected-event distinction, the stable separate animated cursor surface plus exact DirectComposition shimmer/shadow and live locale/theme-accent updates, window-level AUMID resolution for multiplexed hosts, richer transient-window screenshot state, official plugin discovery, Chrome/Excel/PowerPoint controls, transfer approvals, Linux AT-SPI/Wayland parity, and platform equivalents for picture-in-picture/locked use |
| In-app browser | partial | A native GPUI panel owns bounded tabs, URL/navigation state, JPEG frame streaming, keyboard, pointer, and scrolling against a supervised isolated Edge/Chrome/Chromium profile. The same browser runtime exposes the stable `codex-browser-use-*` local endpoint, native-endian length-prefixed JSON-RPC, per-session routing, stable `getInfo` identity, tab claiming/finalization/marking, target attach/detach, bounded cached expressions, safe CDP execution, cursor movement, and filtered CDP notifications; it starts before each interactive turn so the official Browser client can discover it without the panel being opened first. Agent downloads remain deny-by-default behind one exact 10-second URL grant tied to the requesting tab's bounded frame tree, while native pointer/keyboard input receives a separate one-shot user grant. Files stage inside the owned profile, receive collision-safe names, finalize without overwriting into the selected download directory, and emit the stable `onDownloadChange` `started`, `in_progress`, `complete`, `failed`, and `canceled` states. The exact stable Downloads settings persist a custom Location plus Reset and `Ask where to save downloads`; both hot-apply to the supervised runtime. Prompted user downloads preserve the original authenticated Chromium transfer in staging, use native Save As, permit replacement only for the exact user-confirmed destination, and cancel cleanly without re-fetching the URL. A bounded persistent 200-item native manager exposes progress, acknowledgement, capability-gated Pause, Resume, Cancel, Open, Show in folder, Open folder, and Remove from history through opaque IDs rather than arbitrary UI paths. Terminal history uses the codexRS single-writer SQLite store, migrates the existing schema, restores in bounded pages, rechecks file existence, and deliberately omits source URLs. Pause and Resume keep the original authenticated transfer: a bounded hidden Downloads WebUI target uses Chrome's Mojo handler or Edge's discovered native page handler, matches only the staged path/approved URL, and closes after each action. Stable `visibility`, `viewport`, and tab-scoped `pageAssets` capabilities are advertised with their exact descriptions. Visibility requests pass through the normal core reducer and are synchronized back from the selected task's actual GPUI panel, including stable-style pending-open reads; explicit viewport breakpoints survive ordinary panel resizes, and reset returns to the latest real surface dimensions. Windows uses bounded 64 KiB named-pipe buffers so event bursts cannot stall behind the transport default. Framing, routing, visibility show/hide/pending state, viewport bounds/override/reset, grant confinement, collision-safe and explicitly confirmed finalization, history bounds, filenames, preferences, persistence migration/restoration, and the bridge-to-runtime path have focused tests, including an ignored live installed-browser agent-download, prompted user-download, file-upload round trip, and the production pause/resume controller on installed Chrome and Edge. The unmodified installed stable Browser client completed `getInfo`, tab discovery, direct and cached CDP, tab creation/marking, user-tab discovery, finalization, visibility get/set, viewport override/reset, `allowDownload`, the exact completed-download event flow, a real `pageAssets` inventory plus inline-SVG bundle, and its guarded intercepted file-chooser upload flow against the native bridge. The Browser settings page, native directory picker, Save As preference, and download-manager modal were exercised in the built GPUI application and restored to the original state afterward. Public Prod deliberately leaves `BrowserUser.history` disabled, so codexRS does not advertise that internal-only override. Add local-preview comments and annotations, site permissions, exact remaining Browser settings, and repeatable official-client smoke coverage; WebMCP remains gated to internal/Public Beta builds in stable |
| Scheduled tasks | missing | Suggestions, manual/chat-assisted creation, schedule editor, run history, unread/archive states, plugin templates, pause/edit/delete, and notifications |
| Marketplace admin-disabled install | done | Public `plugin/list` availability preserves `DISABLED_BY_ADMIN`; catalog actions stay disabled with the recovered `Access is turned off by your admin` tooltip, and plugin details show `Disabled by admin` instead of collapsing policy into generic unavailability |
| Plugins marketplace | partial | Stable `plugin/list`, `plugin/read`, install/uninstall, search, refresh, and `config/batchWrite` enable/disable work end to end. The OpenAI/Shared/Created/Workspace/Local directories use exact marketplace kinds; the native catalog has bounded remote logos with App metadata enrichment, original raster hero artwork, stable Featured/category ordering, filters, restored-route loading, and a policy-driven install confirmation that fails closed when the official interstitial policy is true, null, or absent, plus installed-plugin Skill toggles and Information links. Installed cards expose the stable `Try now`/`Manage`/`Uninstall` menu, and `Try now` starts a native chat with the plugin's bounded default prompt. Manage has compact single-column installed Plugin and App lists with config-backed enable/disable switches, plus installed-Skills and custom-marketplace tabs. Apps load through bounded paginated `app/list`; Manage also reads the bounded `app/installed` runtime snapshot for the selected `threadId`, intersects it with directory metadata, applies runtime enabled/callable state, and ignores stale scoped results. `app/list/updated` invalidates the current snapshot and refreshes only when no App load is already pending. Connected and connectable entries, name/description search, and bounded remote logos are preserved. Selecting an App issues exact bounded `app/read` with `includeTools`, rejects stale responses, and opens the stable-shaped native tools dialog with loading/error/retry/empty states, action summaries, bounded tool metadata, enable/disable, and a validated `Manage on ChatGPT` URL. Its `Try now` action matches stable by opening a native new chat prefilled with the escaped `app://` mention, and stays disabled until the App is enabled, connected, and callable. Connectable Apps expose the stable validated browser fallback fragment, while connected Apps expose its matching management fragment. Successful plugin installs preserve the official `appsNeedingAuth` response and open a bounded native `Connect apps to finish setup` dialog; each App uses the existing validated ChatGPT connect URL and the dialog never claims browser authentication has completed. Manage MCPs merges effective `mcp_servers` config with bounded full-detail `mcpServerStatus/list`, searches key/display name, separates runtime-only servers under `From plugins`, and preserves advertised server metadata, tools, JSON schemas, resources, and resource templates in a bounded native inspector. Advertised resources have an explicit read action through `mcpServer/resource/read`; text is bounded for storage/rendering and binary payloads are summarized without retaining raw blobs. It writes `mcp_servers.<key>.enabled` and completes OAuth through `mcpServer/oauth/login`, its typed completion notification, and `config/mcpServer/reload`. Its native Add/Update/Uninstall editor now matches the stable STDIO and Streamable HTTP fields, stable key generation, replace/null config writes, immutable transport-on-edit notice, project-origin read-only controls, startup status updates, and reauthentication-required reconnect state. GitHub/Git/local sources can be added, searched, upgraded individually or together with bounded partial-error reporting, and removed after confirmation through typed `marketplace/add`, `marketplace/upgrade`, and `marketplace/remove`. The Plugins directory, Manage tabs, Add marketplace dialog, and long MCP editor follow the stable viewport caps and were exercised at `480×600`; the editor also reflowed from 442 to 640 px while open at 800 px. Create plugin/skill hands off to a prepared native chat. Add native first-party App OAuth/no-auth callback completion when the official app-server exposes that private ChatGPT/Electron flow, action read/write grouping when exposed by the official protocol, remaining plugin disclosure states, sharing/editing, richer filters, hero rotation, and admin states |
| Skills | partial | Stable `skills/list` discovery, browse and Manage search, scope/path metadata, partial scan errors, refresh/invalidation, and `skills/config/write` enable/disable work against the selected project context. Manage loads installed Skills with counts and normalized native paths. Enabled absolute-path skills now register bounded native `skill:<path>` slash rows with stable display name, short description, `Skills` grouping, and scope label; exact name/display-name submission and pointer selection reuse the typed `UserInput::skill` attachment path. Non-empty `@` queries expose the same enabled catalog in a keyboard-selectable `Skills` section after Plugins, Desktop apps, and Apps; selection removes only the active mention token, preserves surrounding text, and rejects disabled, relative, unknown, and duplicate attachments. Slash, pointer, keyboard, surrounding-text, disabled-skill, and 1280×820/480×600 layout paths were exercised against an isolated native fixture. Add recommended/install flows, creation and detail surfaces, extra roots, and higher-priority override guidance |
| MCP and apps | partial | Manage Apps and Manage MCPs now cover bounded list/search, App tool inspection through `app/read`, App `Try now`, enablement, validated external management and browser install/connect fallback, full-detail MCP tool/schema/resource/template inspection, deliberate bounded resource-content reads, config editing, OAuth, reload, startup/reauthentication, and project-origin guards. Enabled accessible Apps from the same bounded catalog are available as stable `app://` composer mentions, with plugin-backed duplicate aliases excluded before rendering. URL, standard JSON-Schema form, and extended OpenAI image-picker modes of MCP elicitation use typed stable request/response contracts, bounded native interruption surfaces, exact actions, and typed accepted content. Add native App OAuth/no-auth callback completion when exposed by the official app-server and sandboxed MCP App surfaces |
| Artifacts and files | partial | The bounded native Outputs inspector follows stable discovery for completed file changes, `::output` citations, and generated images; deduplicates latest-first; hides directives from the response; and provides the exact empty/create menu, native Markdown/CSV/TSV and PNG/JPEG/WebP previews, the stable 40 MiB guard, workspace confinement, and Reveal. Generated images also render as stable-shaped bounded timeline previews; selecting one opens the same native viewer and Reveal flow. The generated-image header exposes the stable `Download` action, suggests `Codex Image {date}.{extension}` in the native Save As dialog, copies only a known workspace-confined generated image, and reports the exact generic failure without exposing paths or provider payloads. The visible action, suggested filename, Downloads destination, and byte-identical saved copy were exercised through Computer Use against an isolated fixture. Completed response outputs render as stable-style end-resource cards with the exact file-type labels, a three-item disclosure limit, and Open behavior. Opening one switches to a 680 px artifact viewer with a dedicated file header and Back flow. Exact `Ctrl/Cmd+P` workspace search reuses the typed fuzzy-session lifecycle, stable generated-directory exclusions, keyboard/pointer selection, and bounded path confinement; files open in the native side viewer while directories open in the platform file manager. The no-workspace guard, unified-menu drill-in, `node_modules` exclusion, both selection paths, native source preview, and directory handoff were exercised against an isolated fixture. Create actions start prepared `@Documents`, `@Presentations`, `@Spreadsheets`, or `@Sites` chats. Add host-side existence filtering, PDF and Office renderers, presentation navigation/zoom, generated-image edit/canvas actions, AVIF/GIF, website/HTML detection, syntax-highlighted source parity, diff counts, native-app target menus, general artifact downloads, and full-screen/tab controls |
| Sites | missing | Create, preview, annotate, version, share, publish, and return-to-chat flows |
| Visualizations | missing | Native chart/report rendering, source inspection, interaction, export, and editor handoff |
| Appshots | missing | Foreground-window capture, destination policy, hotkey, sound, preview, and text/offscreen context |
| Settings | partial | The route replaces app navigation with the stable-shaped 274 px Settings shell: `Back to app`, a bounded search field focused by Ctrl/Cmd+F, Personal/Integrations/Coding/Archived groups, filtering, and the no-results state. Working native sections are `General`, `Appearance`, `Personalization`, `Keyboard shortcuts`, `Profile`, `Usage & billing`, `Plugins`, `MCP servers`, `Browser`, `Computer use`, `Connections`, `Configuration`, `Hooks`, `Git`, `Code review`, `Worktrees`, and `Archived chats`. `Profile` owns account identity, plan, and authentication controls, while `Usage & billing` retains credits and bounded general rate-limit windows over the same typed account snapshot. The MCP section uses the exact stable title and `Connect external tools and data sources` subtitle, a dedicated bounded search with exact connected/no-match empty states, and the existing typed app-server refresh, authentication, enablement, inspection, resource-read, Add, Update, and Uninstall paths instead of a second configuration implementation. Its Settings navigation, custom-server form, disabled Save state, search result, Escape return, and `MCP` command-menu transition were exercised through Computer Use against an isolated official app-server fixture. The Plugins section uses the exact `Manage plugins, skills, and MCPs` subtitle, opens the existing typed manage-only Plugins/MCPs/Skills/Marketplace catalog without duplicating backend state, keeps tabs reachable at 480 px, and its navigation, search, tab switching, and no-match state were exercised against an isolated official app-server fixture. The Configuration section follows the stable `agent-settings-CzaETiyt.js` contract: layered Project/User/Admin scopes come from bounded official `config/read` results, restrictions come from `configRequirements/read`, and editable `approval_policy`, `sandbox_mode`, and conditional `sandbox_workspace_write.network_access` values use versioned `config/batchWrite` followed by an authoritative reload. It preserves project trust/disabled reasons and managed-origin locks, exposes the exact stable option copy, and links to the official config reference without opening live Codex files directly. Focused protocol, reducer, snapshot, and Settings-search tests cover scope targeting, allowed-value restrictions, mutation refresh, and managed values. Computer Use against an isolated official app-server profile exercised navigation, approval and sandbox menus, Project/User/Admin scope grouping, disabled project-trust copy, conditional network rendering, and project/profile restoration across process restart. Personalization is backed by official config, supports the exact None/Friendly/Pragmatic choices, feature-gated memory controls, and explicitly propagates personality through normal thread and turn paths. The Hooks section loads bounded project contexts through official `hooks/list`, groups exact config/plugin/project/other sources and lifecycle events, exposes stable loading/error/empty and trust-review copy, keeps managed and untrusted toggles disabled, and writes only `hooks.state.<key>.enabled` or `trusted_hash` through `config/batchWrite`; focused protocol, reducer, config-shape, and Settings-search tests cover the trust boundary and mutation refresh. The Worktrees section restores the stable Worktree root copy, validates and persists only absolute native paths in bounded codexRS-owned storage, routes managed-fork creation through the saved root, refreshes the selected repository's bounded native worktree inventory, lists linked conversations, and reuses existing navigation and `Use this worktree`; stable snapshot-backed auto-pruning and targeted deletion remain pending rather than exposing unsafe or dead controls. The Git section uses the exact stable branch-prefix, force-with-lease, draft-PR, merge-method, commit-instructions, and PR-instructions copy and ordering recovered from `git-settings-C6oe-K42.js`; it persists one bounded codexRS-owned value, wires those choices into branch naming, push, PR creation, merge emphasis, and bounded generation prompts, supports blur and Ctrl/Cmd+S save semantics, and was exercised through edit/save/restart persistence and dirty states in an isolated official app-server fixture. The Code review section persists the existing `Inline`/`Detached` delivery preference and sends it with public `review/start`; its navigation, exact row copy, search, both choices, and restart persistence were exercised through Computer Use against an isolated profile. The exact `Disable Git-Based Review` toggle is persisted with the other Git preferences; enabling it immediately switches the review panel to `Last Turn`, hides Unstaged/Staged/Branch, and prevents those Git-backed sources from dispatching until the setting is disabled. The archived section uses bounded app-server-owned pages without reading live Codex storage directly. Its stable-derived controls default to `All chats`, `All projects`, and `Updated`; provide Local/Cloud type filtering, project and projectless-chat filtering, Updated/Created/Alphabetical sorting, bounded fuzzy search, automatic project grouping with counts, pagination, exact loading/error/empty/no-match copy, unarchive, icon-only single deletion, project-menu `Delete all in project`, and header `Delete all` backed by a bounded all-pages scan. Every permanent deletion uses the pinned `thread/delete` contract, exact stable confirmation copy, a keyboard focus-trap around Cancel/Delete, sequential app-server acknowledgement, pluralized success only after every request succeeds, and a bounded refresh after partial failure. Local timestamps preserve both created and last-updated protocol fields; Cloud remains an honest empty filter until the private ChatGPT task source has a native contract. The command menu opens its supported section subset, including the working Connections runtime surface, while Ctrl/Cmd+comma and the sidebar entry resolve to General. Appearance uses the installed System/Light/Dark preview assets and exact ordering, changes the real global GPUI theme immediately, follows later OS appearance changes in System mode, and persists one bounded versioned codexRS-owned value. The functional custom editor covers accent/background/foreground colors, UI/code fonts, translucent sidebar, contrast, raw and URI-encoded `codex-theme-v1:` import/copy, and the installed renderer's exact 28 code-theme IDs across 43 supported light/dark variants with their derived palette seeds and theme-specific font/opacity overrides. Preferences cover pointer cursors, bounded UI/code font sizes, and color/symbol diff markers. Reduced motion is functional: `On` immediately removes the used Switch and Checkbox transitions and the scrollbar idle fade, while `Off` preserves them. Legacy stored `System` values resolve to `Off` without being rewritten because the native Windows/Linux shell does not yet expose a shared OS motion signal. Its 768 px content geometry, 24 px split-diff rows, marker bars, compact controls, light Proof palette, and dark Codex palette were compared with cached official reference screens in the same `1280×938` viewport after each visual correction; both variants and restoration across process restart were exercised in an isolated app-server fixture. The native primary window now matches the stable `1280×820` outer default on Windows and persists one bounded versioned codexRS-owned normal placement plus maximized state. Undersized values are clamped to the stable `480×600` storage minimum, off-display values fall back to centered defaults, Windows checks exact monitor work areas, and Linux falls back to bounded GPUI display geometry. Move/close/restart restored the same `180,286 / 1280×820` window and maximize/close/restart restored `2048×1104` through Computer Use in an isolated profile. The native 34 px title/menu shell now matches the stable control order and Windows 46 px system-button geometry; File/Edit/View/Help expose only working local actions or validated external links, and platform-specific Ctrl/Cmd accelerators no longer surface Windows `Win` bindings. The runtime shell now follows the recovered 960/720 px breakpoints, uses a 275 px sidebar, renders narrow navigation and right-side panels as overlays, keeps compact task actions and composer controls reachable, and preserves the main content width. Computer Use verified the exact default and minimum geometry, menus, dragging, maximize/restore, 1280→800→480 transitions, right-panel hide/restore, narrow Settings and command menu layouts, and exact `908,325 / 480×600` placement restoration in an isolated profile. Read-only stable `app.asar` inspection recovered the remaining visible section registry: `Import`, `Codex Micro`, `Appshots`, `Cloud preferences`, `Cloud environments`, `Computer history`, `Environments`, `Voice`, `Pets`, and gated `Debug`. The current stable bundle registers `skills-settings` but deliberately hides it from the visible Settings registry; the separate `/skills` route remains the parity target and is already implemented natively. Add remaining visible sections only with their working host contracts, plus real backdrop translucency, system-font stack presentation, and syntax-highlighting parity with host scoping, policy states, validation, and persistence |
| Account and usage | partial | Native `account/read`, `account/login/start`, `account/login/cancel`, `account/logout`, `account/rateLimits/read`, and typed `account/usage/read` handling now drives separate stable-shaped settings surfaces over one typed official account snapshot: `Profile` for ChatGPT identity, plan, sign-in/cancel, and confirmed logout; `Usage & billing` for credits, a read-only available usage-limit-reset count, bounded general usage-limit windows, an ephemeral five-field ChatGPT token-activity summary, and bounded daily token-usage rows from the public `dailyUsageBuckets` response. The account snapshot loads on every app-server connection; an empty workspace shows the existing ChatGPT login/cancel flow only after a fresh `requiresOpenaiAuth` response, clears stale login IDs across reconnects, and exposes a bounded account-load retry without duplicating offline recovery. Browser, explicit device-code, API-key, and experimental Amazon Bedrock sign-in use their exact pinned public request variants and accept only matching responses. API-key and Bedrock-key inputs are masked and remain UI-local until moved into bounded one-shot backend commands; the owned secret wrapper redacts Debug output and overwrites its byte buffer on drop. Bedrock region input is bounded, managed-credential state is preserved from `account/read`, and a successful Bedrock login gracefully restarts the supervised official app-server before any new model request. A failed restart never launches a second runtime and reports that credentials were already saved without exposing provider details. Secrets never enter reducer state, effects, codexRS storage, snapshots, status copy, or logs. One-time codes remain ephemeral, external authorization URLs are validated before opening, and terminal lifecycle events clear login material without exposing provider errors. The non-required first-run guard was exercised through Computer Use against an isolated official app-server profile; required, failure, reconnect, device-code, invalid-response, and cancel-failure states have focused coverage. A compact Tasks notice identifies the typed Bedrock credential mode, keeps Profile credential management reachable, and uses safe save/restart progress copy. Add reset-credit detail UI and guarded upgrade/purchase/auto-reload billing entry points |
| Feedback | done | The exact stable `Feedback` command and `/feedback` chat command open a native `Share feedback` dialog with the five recovered category IDs, required-details validation, default-on current-session logs, and no browser-tabs control when no browser surface exists. Submission uses the pinned typed `feedback/upload` request through the supervised official app-server, carries only bounded details, the selected thread ID when present, and the app version tag, keeps provider payloads redacted, and uses the exact stable `Feedback uploaded` and retry copy. The Help menu and editable App-group unassigned command route to the same dialog |
| Notifications and tray | partial | Successful background chat completion shows a bounded in-app banner with Open and Dismiss; generic status banners remain independent. The native main-window title and Windows notification-area tooltip report the same bounded count of non-selected chats that are running or waiting for approval and return to `codexRS` when none remain. Windows shows one quiet-hours-respecting native notification-area completion alert, while Linux sends the same best-effort completion alert through the freedesktop notification service without blocking the UI. Add unread/recent tray groups, deep links, sounds, badges, and scheduled-task behavior |
| Remote control and SSH | partial | Native Connections UI supports bounded public `remoteControl/status/read`, `remoteControl/enable`, `remoteControl/disable`, pairing start/status, paginated client discovery, and client revoke. Keep-awake, SSH profiles, remote chats, and handoff remain unavailable until a public app-server contract exists |
| Cloud environments | missing | List/detail/create links, repository/machine metadata, target selection, environment connection state, and cloud execution |
| Voice | missing | Dictation, realtime voice, handoff target, stage layout, settings, and accessibility states |
| Personalization and memory | partial | The native Personalization settings route follows the pinned renderer's `Friendly` / `Pragmatic` tone contract and also fixes its known `None` omission using the pinned app-server's generated `none` / `friendly` / `pragmatic` enum. It reads the current `personality` with the legacy `model_personality` fallback through official `config/read`, writes only `personality` through `config/batchWrite`, and supplies the resolved value explicitly on normal `thread/start` and `turn/start`. The 260 px menu preserves the stable labels, descriptions, and right-side selection mark. When the app-server exposes `features.memories`, the same page uses the recovered default values and exact public keys for enabling memories plus the tool-assisted-chat policy, clears the legacy external-context key on mutation, and reloads authoritative config after every write or managed override. The Memory card also exposes exact `Reset memories` / `Delete all ChatGPT memories` copy, the stable `Reset all memories?` confirmation, and typed no-parameter `memory/reset` with exact success and bounded error status. Per-chat memory controls are available through `/memories`: new-chat choices are carried only into that `thread/start`, while current-chat generation uses the stable typed mode mutation. The exact stable `personalitySettings` command opens this route with an unassigned Configure-group shortcut. Focused protocol, reducer, legacy/default, Settings-search, command-registry, and isolated pinned-app-server smoke tests cover the public contract. Add Custom instructions, retention controls, computer history, Chronicle permissions, and other memory surfaces only when their currently private host contracts have a native public boundary |
| Pets and Codex Micro | missing | Overlay lifecycle, device integration, commands, settings, mini-game/composer, sound, and accessibility |
| Import and migration | partial | The native Personal → Import route uses the pinned official app-server's typed `externalAgentConfig/detect`, `externalAgentConfig/import`, and `externalAgentConfig/import/readHistories` methods for Claude Code, Claude Cowork, and Cursor. It preserves the stable provider-review and item-review flow, supports per-provider and per-item selection across settings, instructions, MCP servers, hooks, commands, skills, plugins, subagents, memory, and recent sessions, groups selected imports by source, and follows typed progress/completion notifications without reading external-agent or Codex files directly. Detection, nested migration details, import batches, histories, connector candidates, provider errors, and active state are bounded; connection loss and partial start failures remain recoverable. The settings page renders loading, empty, found, running, failure, history, and connector-auth attention states plus bounded history expansion and links into the existing Skills, Plugins, MCP, Hooks, and chat surfaces. The protocol/reducer/notification paths have focused tests, the pinned `0.146.0-alpha.3.1` app-server accepted all three source detections, an empty import, and history reads in an isolated `CODEX_HOME`, and the found/empty history layout was exercised in the native app with an isolated external-agent fixture. Add exact unsupported-project reporting when the public protocol exposes it, richer per-history warning correlation, startup migration prompts, and remaining private-host recovery affordances |
| First run and updates | partial | Help → `About codexRS` opens one native fixed-size `380×360` floating window centered on the parent, reports the package version and project copyright, focuses the existing instance when reopened, and closes through OK, Escape, or the native close control. A failed initial backend or app-server connection now remains visible in the empty Tasks area; the app-server path uses the existing Retry action, while a missing backend directs the user to restart. A signed-in empty workspace offers `Open folder`, reusing the native folder picker and existing explicit-workspace handoff. Add fuller welcome, dependency setup/diagnostics, update prompt, and quit confirmation |
| Keyboard and accessibility | partial | The native command menu supports Ctrl/Cmd+K, Ctrl/Cmd+Shift+P, Ctrl/Cmd+G, and the stable Ctrl/Cmd+P Search files drill-in, with arrow selection, Enter, pointer activation, and Escape. Its verified stable registry subset routes New standalone chat, Open folder, Back, Forward, Find, Toggle pin, Previous chat, Next chat, Toggle sidebar, Toggle bottom panel, Toggle Review panel, Commit or push, Create PR, Create draft PR, Create branch, Merge PR, Open PR on GitHub, Open review tab, Open terminal, Go to skills, Force reload skills, MCP, the supported Settings sections, Feedback, Log out, and Process manager into existing native surfaces. `Feedback` uses the exact `Send product feedback to the ChatGPT team` description, App grouping, and unassigned default. `Log out` uses the exact stable title, `Sign out of ChatGPT` description, App grouping, and unassigned default; it appears only for an idle signed-in account and opens the existing exact confirmation dialog before the typed `account/logout` request. The separate stable `toggleReviewTab` row uses exact `Toggle review` / `Show or hide Review for the current Git-backed chat` copy, Panels grouping, and an unassigned default. It is active only on a selected local Git task; closing Changes hides it, while any other right-side state switches specifically to Changes and closes a competing right-docked terminal. Stable `toggleMaximizeSidePanel` uses exact `Toggle maximize side panel` / `Expand or restore the side panel` copy, General fallback grouping, and an unassigned default; it dispatches only while a task-side panel is open. All six stable `git.*` rows use their exact titles/descriptions, Project grouping, unassigned defaults, editable bindings, and command-menu IDs; linked-PR commands are surfaced only when their guarded native action is available. Stable `Approve request` and `Decline request` rows use their exact descriptions and default `Enter`/`Escape` bindings, remain editable, and dispatch only while a selected-chat approval card owns the active request. Stable `copyDeeplink`, `copySessionId`, and `copyWorkingDirectory` use the exact Windows/Linux defaults Ctrl/Cmd+Alt+L, Ctrl/Cmd+Alt+C, and Ctrl/Cmd+Shift+C and resolve only the selected chat; the macOS-only `copyConversationPath` remains intentionally absent from the Windows/Linux registry. Stable `forkThread`, `forceReloadSkills`, `openSkills`, `keyboardShortcuts`, and `mcpSettings` are also editable with their exact titles/descriptions, General/Skills/Configure groups, and unassigned defaults; each reuses the existing native action, and `forkThread` is enabled only with a selected chat. Both shortcut surfaces now use the recovered stable `Chat → Navigation → Panels → Project → Skills → Configure → App → General` comparator, including the special chat-command priority and ID ordering; fallback commands such as `Copy as Markdown` correctly live in General. The unfiltered command rows now use the stable compact one-line density while filtered results retain descriptions; the leading `New chat`, `Open folder`, and workspace-aware `Search files` Suggested order followed by the dynamic `Settings` group was checked side by side with the official app. Session-local Back/Forward mirrors stable with a bounded 256-location route/chat history, forward-branch truncation, disabled titlebar controls, exact command labels/descriptions, Ctrl/Cmd+bracket shortcuts, and MouseBack/MouseForward capture. Route and selected-chat replay, command execution, disabled states, branch truncation, and the always-visible titlebar controls were exercised against an isolated official app-server fixture; shortcut key matching and history bounds have focused tests. Stable `Find` uses Ctrl/Cmd+F, the exact `Search the current chat` description and `Search chat…` field, bounded Unicode-safe nonoverlapping matching across the loaded timeline, per-occurrence result ordinals, Enter/Ctrl/Cmd+G next, Shift+Enter/Shift+F3 previous, Escape close, native source-range warning highlights for plain-text and rendered GFM Markdown, stronger active-occurrence styling, active-message reveal, and right-panel-aware placement. Computer Use exercised the shortcut, command-menu path, counter, repeated-match navigation, exact active highlighting through bold text, links, and inline code, preserved nested styling, Escape cleanup, and Markdown reveal against an isolated native fixture. Find now automatically follows bounded remaining official `thread/items/list` pages for the active query, preserves the current occurrence while pages arrive, reports searching or offline waiting state, restarts on query changes, cancels on chat/route/close, and stops on the 2,000-item, 32-page, 1,000-match, repeated-cursor, or load-failure guards. The second-page-only match path was exercised end to end against an isolated app-server fixture; focused tests cover cursor and page/item bounds. Previous/Next follow the displayed pinned/project order without wrapping and accept the stable Ctrl/Cmd+Shift+bracket bindings plus Windows/Linux Ctrl+PageUp/PageDown; the alternate Page keys, empty-selection behavior, and both directions were exercised against a two-chat isolated app-server fixture. The exact stable `thread1` through `thread9` commands expose `Go to chat N`, `Open the visible chat in this shortcut slot`, and Ctrl/Cmd+1 through Ctrl/Cmd+9; they select the same displayed pinned/project order and safely do nothing when the slot is empty. Their registry metadata, editable persistence, visible ordering, and bounds have focused tests. The exact session-local `Toggle sidebar` action, `Show or hide the sidebar` description, Ctrl/Cmd+B binding, changing open/closed titlebar icon, status placement, and reclaimed main/terminal width were exercised through the shortcut, button, and command menu against the same native fixture. Ctrl/Cmd+J implements the stable `Toggle bottom panel` contract without conflating it with `Open terminal`; Ctrl/Cmd+Alt+B implements the stable `Toggle Review panel` contract while preserving the active right-side tab; Ctrl/Cmd+Alt+O, Ctrl/Cmd+Alt+P, Ctrl/Cmd+Shift+G, Control+backquote, and Ctrl/Cmd+comma are also wired, including Windows punctuation-key names and Russian-layout equivalents. Titlebar popup menus render the first effective binding and remove labels for unassigned commands instead of leaking static defaults. The Reset Memories and Reset all keyboard shortcuts confirmations each receive focus once per opening and confine Tab and Shift+Tab to Cancel and Reset. Add the remaining stable commands and Settings sections, complete focus order, screen-reader labels, OS-level reduced-motion following, and contrast |
| Active keyboard shortcut reference | done | The exact stable `Keyboard shortcuts` dialog opens from Help and the effective Ctrl/Cmd+/ action, lists only bindings active in codexRS in stable `Chat`, `Navigation`, `Panels`, `Project`, `Skills`, `Configure`, `App`, `General` order, renders platform-aware keycaps, and searches bounded title/description/ID text with stable no-results copy. Its 600×720 preferred surface and viewport-bounded `480×600` layout were exercised through Computer Use. With a non-empty query, the first Escape clears search and stays open; the second Escape closes the dialog. `/settings/keyboard-shortcuts` provides bounded text and exact keystroke-prefix search, capture with conflict feedback, set/replace/Shift-append/remove/per-command reset, confirmed reset-all, persisted versioned overrides, effective runtime dispatch including two-chord sequences, dynamic titlebar-menu labels, and correlated stable failure copy with optimistic-state rollback. The stable unassigned `Copy as Markdown` command is present in the editable registry and becomes immediately dispatchable after assignment. Computer Use exercised conflict, replacement, old-binding suppression, new-binding dispatch, active-reference and titlebar-menu rendering, Keys/Escape behavior, reset confirmation/cancel, and persistence across restart; focused reducer tests cover persistence failure rollback |
| Windows | partial | Core smoke path passes; complete packaging, signing, native browser/Computer Use integration, notifications, and accessibility |
| Linux | platform | CI builds; Browser and Outputs resolve the bounded standard XDG Downloads directory; the installable per-user desktop entry and every native window share `com.codexrs.CodexRS` for correct shell grouping; background completion uses the freedesktop notification service on a bounded best-effort worker; Computer Use provides only X11/XWayland screenshot observation when `DISPLAY` is set (no text, input, launch, persistent approval, overlay, or interruption monitor); complete Wayland portals, desktop matrix, packaging, tray/badges, global shortcuts, and parity smoke tests |

### Multi-file diff baseline

Whole-source Changes views group bounded patches by file, keep every file
expanded by default, and expose keyboard-focusable per-file fold controls.
Unified and Split layouts apply across Last Turn, Uncommitted, Committed, and
Branch sources; exact Unstaged and Staged file scopes retain their existing
review and staging controls. Syntax highlighting, inline comments, per-hunk
actions, and guarded revert controls remain open.

### Keyboard accessibility baseline

Local project rows are native tab stops with Enter/Space activation and a
high-contrast focus outline. Their secondary New chat and Project actions
controls remain separate following tab stops. Broader focus-order,
screen-reader, reduced-motion, and contrast work remains open.

### In-app Browser permission status

The stable-shaped Browser settings now include General Approval, bounded
site-specific Browse/Download/Upload/Debug rules, and the Full CDP access
switch. They persist in codexRS-owned single-writer storage and hot-apply to the
native runtime. Explicit site Blocks are enforced at the bridge for navigation,
the current page and download URL, uploads, and raw CDP, with stable wildcard
and deny-before-allow matching. The official Browser origin, download, upload,
page-asset, and raw-CDP elicitations are intercepted only through their exact
typed MCP metadata. Origin access renders the stable
`Allow once` / `Allow for this site` / `Allow for all sites` card; file transfer
renders `Allow once` / `Allow this conversation` / `Always allow`; raw CDP uses
the generic elevated-risk card and stays denied while Full CDP is disabled.
Existing Block/Allow rules and global modes resolve later prompts without UI.
Persistent and conversation choices return the official
`_meta.persist = "always"` or `"session"` handoff, so the supervised plugin
remains the only writer of its guarded Browser configuration.

## Installed Desktop route inventory

The inspected renderer exposes these routes. Internal routes still count
because they represent visible transitions or recovery states that the native
shell must handle.

| Route | Surface |
| --- | --- |
| `/` | Home/new chat |
| `/automations` | Scheduled tasks |
| `/avatar-overlay` | Pet/voice overlay |
| `/codex-mobile` | Mobile/remote entry |
| `/connector/oauth_callback` | Connector OAuth return |
| `/diff` | Standalone diff |
| `/extension/panel/new` | Extension panel |
| `/first-run` | First-run flow |
| `/inbox` | Inbox/notifications |
| `/library` | Output library |
| `/local/:clientThreadId` | Local thread |
| `/login` | Authentication |
| `/mcp-app/:server/:toolName` | MCP app surface |
| `/plan-summary` | Plan summary |
| `/plugins` | Plugins marketplace |
| `/projects` | Project index |
| `/pull-requests` | Pull requests |
| `/remote-connections` | Remote devices and SSH |
| `/remote/:taskId` | Remote task |
| `/security` | Security settings |
| `/select-workspace` | Workspace chooser |
| `/settings` | Settings shell |
| `/settings/data-controls` | Data controls |
| `/settings/keyboard-shortcuts` | Keyboard shortcuts |
| `/settings/mcp-settings` | MCP settings |
| `/settings/personalization` | Personalization |
| `/settings/usage` | Usage |
| `/sites` | Sites |
| `/skills` | Skills |
| `/welcome` | Welcome |

Query variants on `/automations` cover creation and selected automation state.
The renderer also opens local thread, side-chat, pull-request, browser,
artifact, terminal, and review panels without changing the top-level route.

## Settings inventory

The installed renderer contributes searchable controls for these 26 sections:

| Section slug | Required surface |
| --- | --- |
| `agent` | config scopes, sandbox, approvals, network, dependencies, model features, experiments |
| `appearance` | theme, colors, fonts, sizes, contrast, diff markers, motion, sidebar |
| `appshots` | capture hotkey, destination, sound, walkthrough |
| `browser-use` | approval, data, downloads, permissions, sites, CDP, extensions |
| `chronicle` | computer history, sources, permissions, retention |
| `cloud-environments` | configured cloud environments |
| `cloud-settings` | cloud preferences |
| `code-review` | review delivery |
| `codex-micro` | device and interaction settings |
| `computer-use` | app control, plugins, transfers, sounds, picture-in-picture |
| `connections` | remote devices and SSH |
| `data-controls` | data, history, export, deletion |
| `debug` | diagnostics and developer controls |
| `environments` | environment overview |
| `general-settings` | general desktop behavior |
| `git-settings` | branch prefix, force push, draft PR, merge method, instructions |
| `hooks-settings` | hooks |
| `keyboard-shortcuts` | command bindings and reset |
| `local-environments` | local environment definitions |
| `mcp-settings` | MCP servers, OAuth, resources, and tools |
| `personalization` | personality, instructions, and memory |
| `pets` | pet selection and behavior |
| `profile` | account/profile |
| `usage` | limits, credits, and billing entry points |
| `voice` | dictation and realtime voice |
| `worktrees` | root, auto-delete, limit, linked conversations |

## Protocol progress

The pinned stable schema exposes 89 client request methods. codexRS currently
uses these feature methods:

```text
account/login/cancel
account/login/start
account/logout
account/rateLimits/read
account/read
app/list
config/batchWrite
config/read
hooks/list
marketplace/add
marketplace/remove
marketplace/upgrade
model/list
permissionProfile/list
plugin/install
plugin/list
plugin/read
plugin/uninstall
review/start
skills/config/write
skills/list
thread/archive
thread/delete
thread/fork
thread/goal/clear
thread/goal/get
thread/goal/set
thread/items/list
thread/list
thread/loaded/list
thread/name/set
thread/read
thread/resume
thread/rollback
thread/compact/start
thread/search
thread/shellCommand
thread/start
thread/turns/list
thread/unarchive
thread/unsubscribe
turn/interrupt
turn/start
turn/steer
```

Initialization and server-request responses are implemented separately. Method
count alone does not define parity: methods are added only as part of a visible
vertical slice, with bounded request/response handling and focused tests.

## Delivery order

1. Match the shell: projects, chat lifecycle, sidebar, header, composer,
   model/effort/permissions, and resilient thread execution.
2. Match the coding loop: activity timeline, review/diff, Git, worktrees,
   terminal, outputs, and pull requests.
3. Match agent capabilities: Computer Use, browser, skills, plugins, MCP apps,
   scheduled tasks, remote/cloud, and notifications.
4. Match creation surfaces: artifacts, files, Sites, visualizations, appshots,
   image/audio/voice, and previews.
5. Match the complete settings, onboarding, account, update, accessibility,
   Windows, and Linux contracts.

Each slice must stay native, use bounded data paths, preserve app-server
ownership of existing Codex data, and pass the repository release gates.

## Release-critical parity ledger (GUI-002)

The GUI-002 selection of release-critical rows, per the work order's
rule that explicitly unavailable proprietary behavior is not reproduced
unless a public contract exists and the product acceptance criteria
require it. Verdicts describe the release bar for the first Flauz.app
release; `partial` statuses above stay authoritative for full-reference
parity tracking.

| Row | Release verdict | Remaining delta class |
| --- | --- | --- |
| App-server supervision | green | network-aware diagnostics; remaining experimental methods (enhancement) |
| Projects and chats | green | multi-root sources, unread state, richer metadata (polish); manual ordering closed in GUI-002 |
| Thread execution | green | compaction provenance, richer edit metadata (polish) |
| Composer | green | cloud projects/voice (proprietary, unavailable); remaining slash commands (polish) |
| Model, effort, and speed picker | green | none listed |
| Permission profiles | green | granular/custom editor, sandbox detail, per-project resolution (enhancement) |
| Streaming timeline | green | activity grouping, citation navigation, source aggregation (polish) |
| Approvals and user input | green | connector-specific methods where public contracts expose them (bounded) |
| Repository status | green | none beyond row text |
| Diff review | green | none beyond row text |
| Branches and worktrees | green | none beyond row text |
| Pull requests | green | branch prefix, force push, draft/merge-method options (polish) |
| Terminal | green | none beyond row text |
| Process Manager | green | none beyond row text |
| Computer Use | green (Windows), bounded (Linux) | Linux: X11/XWayland observation only; AT-SPI/Wayland parity is platform work |
| In-app browser | green | none beyond row text |
| Skills | green | recommended/install flows, creation surfaces (enhancement) |
| Plugins marketplace | green | OAuth/no-auth callback completion awaits the official protocol (bounded) |
| MCP and apps | green | App OAuth completion awaits the official protocol (bounded) |
| Artifacts and files | green | PDF/Office renderers, AVIF/GIF, canvas actions (enhancement) |
| Settings | green | remaining sections only with working host contracts (bounded) |
| Account and usage | green | billing entry points (enhancement) |
| Notifications and tray | green | tray groups, badges, sounds (enhancement) |
| Remote control and SSH | green | SSH profiles/remote chats await public contracts (bounded) |
| Import and migration | green | unsupported-project reporting awaits the public protocol (bounded) |
| First run and updates | green | fuller welcome, dependency diagnostics, update prompt (enhancement) |
| Keyboard and accessibility | green | remaining stable commands, complete focus order, screen-reader labels (enhancement) |
| Scheduled tasks | bounded | cloud tasks backend (proprietary, unavailable) |
| Sites | bounded | OpenAI cloud surface (proprietary, unavailable) |
| Visualizations | bounded | OpenAI cloud surface (proprietary, unavailable) |
| Appshots | bounded | proprietary capture flow (unavailable) |
| Cloud environments | bounded | OpenAI cloud backend (proprietary, unavailable) |
| Voice | bounded | proprietary realtime stack (unavailable) |
| Pets and Codex Micro | bounded | proprietary companion surface (unavailable) |
| Windows | deferred to GUI-006 | packaging, signing, integration |
| Linux | deferred to GUI-006 | Wayland portals, packaging, tray, global shortcuts |

Release-critical closure in GUI-002 itself: the Projects and chats row's
manual project ordering gap is closed (persisted manual order within
pinned groups, `Move up` / `Move down` project actions).
