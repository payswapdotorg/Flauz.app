# Linux GUI — Regression Ledger

**Lane:** Linux GUI usability (second Tech Lead — Linux lane).

Every defect observed in the E2B desktop journeys is registered here with
its classification, the journey that surfaced it, the work order that fixes
it, and the E2B re-run that closes it. A defect is CLOSED only when the
same journey re-runs green (or honestly-unavailable) in E2B against merged
`main`.

Severity classes: **P0** crash / persistent unusable GUI / data-loss risk /
startup failure · **P1** major supported journey broken · **P2** important
interaction, discoverability, focus or state problem · **P3** minor visual /
parity / polish · **BOUNDED** explicit architectural/platform limitation
with a truthful user-facing explanation (a capability is NOT bounded merely
because it is difficult).

| ID | Date | Journey | Class | Summary | Evidence | Work order | Fixed @ | Re-run verdict |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| L-001 | 2026-09-21 | E-00 environment provision | P1 | Linux build fails on Ubuntu 22.04 (E2B desktop standard image): `libspa 0.10.0` binding errors against pipewire 0.3.48 headers via `codex-platform → xcap 0.9.7 → pipewire 0.10 → libspa 0.10`. CI validates Ubuntu 24.04 only; `platform-support.md` "other Linux: source expected" is currently false for 22.04. Worked around in-lane by side-loading pipewire 1.0.5 dev+runtime (Ubuntu 24.04 debs, `~/pw10`, PKG_CONFIG_PATH + LD_LIBRARY_PATH). | prov.log; E2B-ENVIRONMENT doc | LAB-001 (Worker A) | — | OPEN |
| L-002 | 2026-09-21 | J-01 start a project (cold start) | P0 | GUI launches (window mapped, focused, WM class `com.codexrs.CodexRS`, 32-bit ARGB visual) but **never presents rendered frames**: window content stays black on (a) E2B Desktop Xfce4/Xvfb :0 and (b) bare Xvfb :98 (no WM), with `LIBGL_ALWAYS_SOFTWARE=1`. Process healthy: 32 threads, event loop idle (epoll_pwait), 2 Hz timer, zero stderr, no crash, no app-server child spawned. `scripts/linux_desktop_smoke.sh` asserts liveness only (exit 124 at 15 s) — never pixels — so this failure class ships green through CI. **ROOT CAUSE CONFIRMED (run-3, 2026-09-21):** GPUI 0.2.2 X11 `find_visuals` prefers the 32-bit ARGB *transparent* visual; on the software-Vulkan stack (lavapipe) every presented frame carries alpha=0 — 100% of window pixels are invisible (wallpaper shows through when composited; black when not). Discriminating evidence: `vkcube` on the same display/visual stack presents opaque pixels fine (24-bit visual). **Fix validated in-lane:** force `visual_set.inherit` (24-bit) via `[patch.crates-io] gpui = { path = … }` with a one-line change in `x11/window.rs` → window Depth 24, own-content xwd mean 246.3 (was 0), full interactive UI (sidebar, composer with keyboard input, command palette, Settings navigation). Remaining for LAB-003: implement as the minimal upstream-shaped accommodation (env-gated or capability-gated visual selection), add the missing logger init (L-005), and upgrade the smoke script to pixel-assertion. | evidence/run1-j01-02, run1-j01-06, run1-j01-05, run3-l002-defect-wallpaper-through.png, run3-vkcube-control.png, run3-l002-fix-inherit-visual.png | LAB-003 (Worker A, queued after LAB-001) | — | OPEN (root cause closed; merge pending) |
| L-003 | 2026-09-21 | E-00 environment provision | P3 | Without `XDG_RUNTIME_DIR` the app runs but logs `error: XDG_RUNTIME_DIR not set in the environment.` (4 lines). The repo's own smoke script sets it; a real desktop session always has it. Minor, but the smoke harness contract should be documented in platform-support. | prov.log | (bundled into LAB-001 scope note) | — | OPEN |
| L-004 | 2026-09-21 | E-00 / J-01 environment + app lifecycle | P1 | The pinned Codex CLI (0.146.0-alpha.3.1) npm wrapper `bin/codex.js` uses **top-level await** and crashes instantly on Ubuntu 22.04's default Node 12.22.9 (`engines: node >=16` is declared but npm installs anyway). Consequence inside Flauz: `codex app-server` is spawned and dies immediately → the app retry-spins (~250 Hz timerfd_settime + epoll churn + 389/389 futex errors per 10 s) with no app-server child visible; with L-002 unfixed the user sees nothing at all. Validated in-lane fix: install the **native platform binary** from `@openai/codex@0.146.0-alpha.3.1-linux-x64` tarball (`package/vendor/x86_64-unknown-linux-musl/bin/codex`) as `codex` — zero Node dependency, exact pinned version, `codex-cli 0.146.0-alpha.3.1` verified, app-server spawns and stays alive (`App-server online` in the UI). Flauz-side defect remains: when the app-server binary fails, the app shows **no error state** (its designed “Couldn't connect to the Codex app-server” card never appears pre-fix) — the retry spin is silent. | run3-appserver-live-transparent-still.png; run3 summary | LAB-003 scope addition (error-state visibility) + harness env fix (native codex binary) | — | OPEN |
| L-005 | 2026-09-21 | E-00 observability | P2 | `codex-app` initializes **no logging backend** (no env_logger/fern/tracing anywhere in the crate) — every GPUI `log::info!/warn!` (visual selection, GPU context, renderer errors) is silently dropped, so Linux GUI startup failures are invisible in stderr/logs. This blinded the entire lane diagnosis. Fix: minimal logger init honoring `RUST_LOG` (default warn). | Lead source analysis (run-3) | LAB-003 scope 2b | — | OPEN |
| L-006 | 2026-09-21 | J-04 capability-gap discovery | P3 (corrected run-4) | **CORRECTED BY RUN-4** — the run-3 "Terminal dead click" was a false positive caused by click-coordinate misses (run-3 clicked above the actual row; run-4 pixel-verified rows and re-tested). Actual behavior (signed-out, zero chats): **Terminal** click shows an honest toast — "Select a task before opening a terminal." — and **Browser** click shows "Open a chat before opening the Browser." Both honest gates ✓. Remaining defect: vocabulary inconsistency — one toast says "task" (Flauz vocabulary), the other says "chat" (internal concept leak). Also verified run-3 claim "composer retains typed text across Settings navigation" — NOT re-verified run-4 (draft persistence across restart is L-010). | evidence/run4-j04-terminal-toast.png, run4-j04-browser-toast.png | Worker B lane (wording pass) | — | OPEN (wording only) |
| L-007 | 2026-09-21 | J-04/J-11 sidebar state | P3 | Sidebar can collapse (toggle control adjacent to "Back to app" on Settings top bar; a miss-click collapsed it during run-4) and the collapsed state shows **no visible restore affordance** — no edge strip, no chevron button. The main empty state even says "Start a new chat or open one from the sidebar" **while the sidebar is hidden** (misleading copy in that state). Recovery paths that DO work: keyboard `Ctrl+B` and the command palette ("Toggle sidebar — Show or hide the sidebar, Ctrl+B"). A user who knows neither is stuck. Fix: visible expand affordance in the collapsed state (thin rail or chevron), or adapt the empty-state copy. | evidence/run4-l007-sidebar-collapsed.png, run4-l007-ctrlb-restore.png, run4-j11-palette-sidebar.png | Worker B lane (UX discipline rubric) | — | OPEN |
| L-008 | 2026-09-21 | J-04 Workflows surface | P2 | Workflows view (signed-out) surfaces a **raw app-server protocol error** as a red banner: "Could not list workflow instances: app-server rejected the request with code -32600", plus "Durable instances: Failed to load". JSON-RPC error codes are not human vocabulary; the surface should show an honest empty/auth state or a worded explanation with a next step. Other sections of the same view ("Teach a workflow", "Published versions") render fine. | evidence/run4-j04-workflows.png | Worker B lane (error-state wording) | — | OPEN |
| L-009 | 2026-09-21 | J-05 add-a-project entry | P2 | The sidebar "Projects" header exposes a **"+" affordance that silently no-ops** signed-out (verified on the main view after pixel-locating the row: no dialog, no inline form, no toast — the section just stays "No projects"). Inconsistent with the sibling "New chat" affordance which shows the honest "Sign in to get started" gate. This is the exact SILENT-NO-OP class the lane forbids: a visible control that does nothing visible. Fix: same honest gate (sign-in or task-creation guidance) as New chat. | evidence/run4-j05-projects-plus-silent.png | Worker B lane (dead-affordance audit) | — | OPEN |
| L-010 | 2026-09-21 | J-03 recover a task | P2 | Typed composer draft ("J-03 draft persistence probe") is **lost on full app restart** — composer comes back empty (placeholder only), and there is **no "we saved your work" / resume prompt** of any kind. The app DOES restore the sidebar expanded state (positive), but the in-progress unsent work is dropped without acknowledgment. In-task resume deeper than the composer requires Codex auth (operator-gated). Fix: persist the active composer draft per chat/app state and restore it on launch (or at minimum surface it as a recoverable artifact). | evidence/run4-j03-draft-typed.png, run4-j03-restart-draft-lost.png | needs work order (Worker B candidate) | — | OPEN |
| L-011 | 2026-09-21 | J-04 toast behavior | P3 | Toasts do not auto-dismiss: the "GitHub CLI (gh) is not installed" toast stayed pinned >15 min across view navigation and app restart, duplicating the Pull-requests panel's inline error and covering sidebar bottom rows (contributed to run-3/run-4 click misses). Calm-information principle: transient notifications should time out or stack in a non-covering region. | evidence/run4-j04-pullrequests.png (toast), run4-j04-terminal-toast.png (second toast replaces first) | Worker B lane (notification discipline) | — | OPEN |
| L-012 | 2026-09-21 | J-01 cold start | P3 (cross-ref UX-003) | First-run promo modal "Introducing GPT-5.6-Sol" has **no visible dismiss affordance** — no X, no "Maybe later"; the only visible action is the primary "Try GPT-5.6-Sol now" (which does dismiss + switch the model — honest in itself). The main TL's lane independently found the modal **swallows all keyboard input on pristine boots** and that **Escape is the verified dismissal** (F2-WORK-ORDERS.md UX-003, dispatched to their Worker D) — so the keyboard path exists but is invisible, and the seen-flag does not carry across version bumps. Lane's remaining gap after UX-003: the *visual* secondary-dismiss affordance. Model promo is also a *discovery path* for J-14 (it switched the composer model to GPT-5.6-Sol). | evidence/run4-j01-launch.png, run4-modal-try-sol.png | UX-003 (main TL, Worker D) + visual-affordance note for Worker B | — | OPEN |

Run-4 positive results (no defect): Repository view honest empty state ("No Git repository detected" + branch/worktree creation inputs); Pull-requests view honest dependency error with actionable buttons ("GitHub CLI setup required" → Install / Check again); Plugins marketplace renders; Settings screen fully navigable (Back to app, search, categories); command palette complete inventory (Suggested: New chat/Open folder; Settings pages; Thread: New standalone chat; Navigation: Search chats/Back/Forward) with working search; ≥2 discovery paths for J-11 (sidebar + palette + promo modal + shortcuts).
